using System;

namespace VhdWrapper.Interop
{
    internal enum GET_VIRTUAL_DISK_INFO_PROVIDER_SUBTYPE
    {
        Fixed = 1,
        Dynamically_expandable = 2,
        Differencing = 3
    }
}
